import styled from 'styled-components';

export const LoginFormContainer = styled.div`
  height: 100%;
  margin-top: -32px;
  display: grid;
  place-content: center;

  > button {
    font-weight: 700;
  }
`;
